name: CD - Hl7v25

on:
  workflow_dispatch:
    inputs:
      bal_central_environment:
        description: Ballerina Central Environment
        type: choice
        options:
        - STAGE
        - DEV
        - PROD
        required: true

jobs:
  build:
     uses: ./.github/workflows/balbuild.yml
     with:
      working_dir: ./hl7v25
      bal_central_environment: ${{ inputs.bal_central_environment }}  