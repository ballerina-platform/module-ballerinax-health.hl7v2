name: daily-build-hl7v22

on:
  schedule:
    - cron: '0 20 * * *'    # 01:30 in LK time (GMT+5:30)
  workflow_dispatch:

jobs:
  build:
     uses: ./.github/workflows/daily-build-executor.yml
     secrets: inherit
     with:
      working_dir: hl7v22
